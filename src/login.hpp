

//
//  This header file contains classes to implement logging in
//  


#include <iostream>
#include <vector>
#include <string>
#include "commonTypes.hpp"
#include "helpers.hpp"


class LoginScreen {

    void renderLogin () {
        ; // show login screen
    }

    bool isValidUser () {
        ; // check if user credentials are valid
    }

    std::vector<std::string> getPermittedSites () {
        ; // gets sites user has permission to view
    }

    void fetchCredentialFiles () {
        ; // fetch mqtt credential files
    }

    void saveCredentialFiles () {
        ; // save mqtt credential files
    }

    void fetchCoAPNetworkIPs () {
        ; // fetch all neru IPs and ports
    }

};